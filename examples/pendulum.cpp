#include <Maxwell.hpp>
#include <numbers>

using namespace maxwell;
using namespace maxwell::metric_literals;

second pendulum_period(meter length) { // return 2.0 * std::numbers::pi * sqrt(length / constants::g); }
}

int main() {}